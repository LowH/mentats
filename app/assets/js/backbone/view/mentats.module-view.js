
Mentats.ModuleView = Backbone.View.extend({

  initialize: function(options) {
    Backbone.View.prototype.initialize.apply(this, arguments);
    debug.log('Mentats.ModuleView', 'new', this);
  }

});
